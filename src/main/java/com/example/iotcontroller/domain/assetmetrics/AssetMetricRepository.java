package com.example.iotcontroller.domain.assetmetrics;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AssetMetricRepository extends JpaRepository<AssetMetric, Integer> {
}